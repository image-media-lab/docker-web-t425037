# 1. ベースとなるWebサーバー（Nginx）の軽量版を使用
FROM nginx:alpine

# 2. コンテナ内の作業ディレクトリを設定（任意）
WORKDIR /usr/share/nginx/html

# 3. 必要に応じて、独自のNginx設定ファイルをコピーする場合などはここに追記します
# 今回はシンプルに、OSのアップデートや特定ツールのインストールが必要になった時のための土台として作成します。